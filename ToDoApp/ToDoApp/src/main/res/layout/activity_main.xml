<RelativeLayout xmlns:a="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    a:layout_width="match_parent"
    a:layout_height="match_parent"
    tools:context=".TodoActivity">
    <ListView a:id="@android:id/list"
        a:layout_width="match_parent"
        a:layout_height="match_parent"
        a:layout_alignParentTop="true"
        a:layout_above="@+id/add_input_layout" />
    <LinearLayout a:id="@id/add_input_layout"
        a:layout_width="match_parent"
        a:layout_height="wrap_content"
        a:layout_alignParentBottom="true"
        a:orientation="horizontal">
        <EditText a:id="@+id/item_input"
            a:layout_width="0dp"
            a:layout_height="wrap_content"
            a:layout_weight="1"
            a:layout_gravity="bottom"
            a:layout_marginBottom="5dp"
            a:hint="@string/add_item_hint"/>
        <Button a:layout_width="wrap_content"
            a:layout_height="wrap_content"
            a:layout_margin="10dp"
            a:onClick="onAddClicked"
            a:text="@string/btn_add" />
    </LinearLayout>
</RelativeLayout>
